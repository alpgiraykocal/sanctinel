services:
  - type: web
    name: sanctinel
    runtime: node
    plan: free
    buildCommand: ""
    startCommand: node --max-old-space-size=400 server.js
    healthCheckPath: /healthz
    autoDeploy: true
    envVars:
      - key: TRUST_PROXY
        value: "true"
