# Runtime snapshot cache — plain JSON stays local; the gzipped snapshot is
# committed (built daily by the refresh-data GitHub Action)
cache/*
!cache/snapshot.json.gz

# Node
node_modules/
npm-debug.log*

# OS / editor
.DS_Store
*.swp

# Local tooling
.claude/
